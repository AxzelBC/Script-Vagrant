# Script Vagrant ArchLinux
Esta máquina virtual (Vagrant) usa ArchLinux e instala automaticamente el Neofetch.

## Instalación y ejecución
~~~sh
$ git clone https://github.com/AxzelBC/Script-Vagrant.git
$ cd Scrip-Vagrant
$ ./install.sh
~~~
